export default {
  helpContent: {
    title: 'EST',
    subtitle: 'Реєстрація через безпечний транспорт',
    overview: 'EST (RFC 7030) забезпечує безпечну реєстрацію сертифікатів через HTTPS з взаємним TLS (mTLS) або HTTP Basic автентифікацією. Ідеально підходить для сучасних корпоративних середовищ, що вимагають стандартизованої реєстрації з надійним транспортним захистом.',
    sections: [
      {
        title: 'Вкладки',
        items: [
          { label: 'Налаштування', text: 'Увімкнення EST, вибір CA для підпису, налаштування облікових даних автентифікації та терміну дії сертифіката' },
          { label: 'Інформація', text: 'URL кінцевих точок EST для інтеграції, статистика реєстрацій та приклади використання' },
        ]
      },
      {
        title: 'Автентифікація',
        items: [
          { label: 'mTLS (взаємний TLS)', text: 'Клієнт надає сертифікат під час TLS-рукостискання — найнадійніший метод автентифікації' },
          { label: 'HTTP Basic Auth', text: 'Запасний варіант з ім\'ям користувача/паролем, коли mTLS недоступний' },
        ]
      },
      {
        title: 'Кінцеві точки',
        items: [
          { label: '/cacerts', text: 'Отримання ланцюжка сертифікатів CA (автентифікація не потрібна)' },
          { label: '/simpleenroll', text: 'Подання CSR та отримання підписаного сертифіката' },
          { label: '/simplereenroll', text: 'Оновлення існуючого сертифіката (потрібен mTLS)' },
          { label: '/csrattrs', text: 'Отримання атрибутів CSR, рекомендованих сервером' },
          { label: '/serverkeygen', text: 'Сервер генерує пару ключів та повертає сертифікат + ключ' },
        ]
      },
    ],
    tips: [
      'EST — сучасна заміна SCEP — віддавайте перевагу EST для нових розгортань',
      'Використовуйте автентифікацію mTLS для найвищої безпеки — Basic Auth є запасним варіантом',
      'Кінцева точка /simplereenroll вимагає від клієнта надання поточного сертифіката через mTLS',
      'Скопіюйте URL кінцевих точок з вкладки Інформація для налаштування клієнтів EST',
    ],
    warnings: [
      'EST вимагає HTTPS — клієнт повинен довіряти сертифікату або CA сервера UCM',
      'Автентифікація mTLS вимагає правильного налаштування завершення TLS (зворотний проксі повинен пересилати клієнтські сертифікати)',
    ],
  },
  helpGuides: {
    title: 'Протокол EST',
    content: `
## Огляд

Реєстрація через безпечний транспорт (EST) визначена в **RFC 7030** та забезпечує реєстрацію сертифікатів, повторну реєстрацію та отримання сертифікатів CA через HTTPS. EST — це сучасна заміна SCEP, що пропонує вищий рівень безпеки через взаємну автентифікацію TLS (mTLS).

## Конфігурація

### Вкладка налаштувань

1. **Увімкнути EST** — Увімкнути або вимкнути протокол EST
2. **CA для підпису** — Вибрати, який центр сертифікації підписує сертифікати EST
3. **Автентифікація** — Налаштувати облікові дані HTTP Basic Auth (ім'я користувача та пароль)
4. **Термін дії сертифіката** — Термін дії за замовчуванням для сертифікатів, виданих через EST (у днях)

### Збереження конфігурації

Натисніть **Зберегти**, щоб застосувати зміни. Кінцеві точки EST стають доступними негайно після увімкнення.

## Автентифікація

EST підтримує два методи автентифікації:

### Взаємний TLS (mTLS) — Рекомендований

Клієнт надає сертифікат під час TLS-рукостискання. UCM перевіряє сертифікат та автоматично автентифікує клієнта.

- **Найнадійніший метод** — криптографічна ідентифікація клієнта
- **Обов'язковий для** \`/simplereenroll\` — клієнт повинен надати поточний сертифікат
- **Залежить від** правильного налаштування завершення TLS (зворотний проксі повинен передавати \`SSL_CLIENT_CERT\` до UCM)

### HTTP Basic Auth — Запасний варіант

Автентифікація ім'ям користувача та паролем через HTTPS. Налаштовується в налаштуваннях EST.

- **Простіше в налаштуванні** — клієнтський сертифікат не потрібен
- **Менш безпечний** — облікові дані передаються з кожним запитом (захищені HTTPS)
- **Використовуйте, коли** інфраструктура mTLS недоступна

## Кінцеві точки EST

Усі кінцеві точки знаходяться під \`/.well-known/est/\`:

### GET /cacerts
Отримання ланцюжка сертифікатів CA. **Автентифікація не потрібна.**

Використовуйте для початкового встановлення довіри — клієнти отримують сертифікат CA перед реєстрацією.

\`\`\`bash
curl -k https://your-server:8443/.well-known/est/cacerts | \\
  base64 -d | openssl pkcs7 -inform DER -print_certs
\`\`\`

### POST /simpleenroll
Подання PKCS#10 CSR та отримання підписаного сертифіката.

Вимагає автентифікації (mTLS або Basic Auth).

\`\`\`bash
# Використання curl з Basic Auth
curl -k --user est-user:est-password \\
  -H "Content-Type: application/pkcs10" \\
  --data-binary @csr.pem \\
  https://your-server:8443/.well-known/est/simpleenroll
\`\`\`

### POST /simplereenroll
Оновлення існуючого сертифіката. **Вимагає mTLS** — клієнт повинен надати сертифікат, що оновлюється.

\`\`\`bash
curl -k --cert client.pem --key client.key \\
  -H "Content-Type: application/pkcs10" \\
  --data-binary @csr.pem \\
  https://your-server:8443/.well-known/est/simplereenroll
\`\`\`

### GET /csrattrs
Отримання атрибутів CSR (OID), рекомендованих сервером.

### POST /serverkeygen
Сервер генерує пару ключів та повертає сертифікат разом з приватним ключем. Корисно, коли клієнт не може генерувати ключі локально.

## Вкладка інформації

Вкладка інформації відображає:
- **URL кінцевих точок** — Готові для копіювання URL для кожної операції EST
- **Статистика реєстрацій** — Кількість реєстрацій, повторних реєстрацій та помилок
- **Остання активність** — Найновіші операції EST з журналів аудиту

## Приклади інтеграції

### Використання est client (libest)
\`\`\`bash
estclient -s your-server -p 8443 \\
  --srp-user est-user --srp-password est-password \\
  -o /tmp/certs --enroll
\`\`\`

### Використання OpenSSL
\`\`\`bash
# Отримання сертифікатів CA
curl -k https://your-server:8443/.well-known/est/cacerts | \\
  base64 -d > cacerts.p7

# Генерація CSR
openssl req -new -newkey rsa:2048 -nodes \\
  -keyout client.key -out client.csr \\
  -subj "/CN=my-device/O=MyOrg"

# Реєстрація (Basic Auth)
curl -k --user est-user:est-password \\
  -H "Content-Type: application/pkcs10" \\
  --data-binary @<(openssl req -in client.csr -outform DER | base64) \\
  https://your-server:8443/.well-known/est/simpleenroll | \\
  base64 -d | openssl x509 -inform DER -out client.pem
\`\`\`

### Windows (certutil)
\`\`\`cmd
certutil -enrollmentServerURL add \\
  "https://your-server:8443/.well-known/est" \\
  kerberos
\`\`\`

## EST проти SCEP

| Особливість | EST | SCEP |
|---------|-----|------|
| Транспорт | HTTPS (TLS) | HTTP або HTTPS |
| Автентифікація | mTLS + Basic Auth | Пароль виклику |
| Стандарт | RFC 7030 (2013) | RFC 8894 (2020, але застарілий) |
| Генерація ключів | Серверний варіант | Тільки клієнт |
| Оновлення | mTLS повторна реєстрація | Повторна реєстрація |
| Безпека | Висока (на основі TLS) | Нижча (спільний секрет) |
| Рекомендація | ✅ Переважний для нових | Тільки для застарілих пристроїв |

> 💡 Використовуйте EST для нових розгортань. Використовуйте SCEP тільки для застарілих мережевих пристроїв, що не підтримують EST.
`
  }
}
