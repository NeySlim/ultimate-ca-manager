# Ultimate Certificate Manager - Backend Dependencies
# Updated: 2026-02-21 - Compatible with Python 3.13

# Flask Core
Flask==3.1.3
Flask-RESTX==1.3.2
Flask-SQLAlchemy==3.1.1
# 2.1 resolves postgresql:// to psycopg 3 and has not been validated yet
SQLAlchemy>=2.0,<2.1
Flask-Migrate==4.1.0
Flask-CORS==6.0.2
Flask-Limiter==4.1.1
Flask-Caching==2.3.1
Flask-Session==0.8.0

# Cryptography & OpenSSL
cryptography==50.0.0
argon2-cffi>=23.1.0
pyOpenSSL==26.4.0
asn1crypto==1.5.1

# WebAuthn/FIDO2 Support
webauthn==2.7.1
cbor2==6.1.2

# TOTP/2FA Support
pyotp>=2.9.0
qrcode>=7.4
Pillow>=10.0.0

# SCEP Support (pycryptodome dropped — SCEP now uses pyca/cryptography's ciphers)
pyasn1==0.6.4
pyasn1-modules==0.4.2

# ACME Support
josepy>=2.2.0
dnspython==2.8.0

# Deploy hooks (#299) — SSH/SFTP push of issued certificates to remote hosts
paramiko==5.0.0

# HTTP & Utils
requests==2.34.2
python-dotenv==1.2.2
python-dateutil==2.9.0.post0
marshmallow==4.2.2
psutil==7.2.2

# Password Hashing
bcrypt==5.0.0

# Production Server
gunicorn==25.1.0

# Email validation
email-validator==2.3.0

# API Documentation
flasgger>=0.9.7

# LDAP/Active Directory Integration
ldap3>=2.9.1

# SSO/SAML Support
python3-saml>=1.16.0
python-jose>=3.3.0

# HSM Support (PKCS#11, Azure Key Vault, Google Cloud KMS)
python-pkcs11>=0.7.0
azure-keyvault-keys>=4.9.0
azure-identity>=1.15.0
google-cloud-kms>=2.21.0

# PostgreSQL support (for HA / multi-node deployments)
# Pre-built binary wheel — no compilation toolchain needed at install time.
psycopg2-binary>=2.9.9

# Redis session store (optional, install with: pip install redis)
# redis>=5.0.0

# Development & Testing
pytest==9.0.3
pytest-cov==7.0.0
black==26.3.1
flake8==7.3.0

# WebSocket Support
flask-socketio==5.6.1
python-socketio==5.16.2
gevent==25.9.1
gevent-websocket==0.10.1
fpdf2>=2.8
matplotlib>=3.8

# Microsoft AD CS Integration
certsrv>=2.1.0
# requests-kerberos is optional: only needed for Kerberos auth to MS CA
# Install manually if needed: pip install requests-kerberos
# (it pulls C-extension Kerberos deps, so it stays out of the default set)
# WinRM admin channel (revoke/unrevoke/publish CRL/inventory on the CA).
# Pure-python wheels, bundled so air-gapped/policy-restricted deployments get
# the admin channel out of the box (#159). Still lazy-imported in
# services/msca/admin_channel.py and the feature degrades gracefully.
pywinrm>=0.5.0
requests-ntlm>=1.3.0
# JKS export support (Java KeyStore).
# pyjks itself is a pure-python wheel, but it declares `twofish` as a dependency.
# twofish has NO prebuilt wheel on PyPI (sdist only) and would force gcc +
# python3-dev to compile its C extension. twofish is only used inside pyjks for
# the BKS UBER keystore format — UCM exports JKS only (never BKS), so twofish
# is dead code at runtime.
#
# To avoid pulling twofish, pyjks is NOT listed here. It is installed by the
# DEB/RPM postinst with `pip install --no-deps pyjks==20.0.0`, after the
# requirements below are satisfied. Its actual runtime deps are listed here
# (javaobj-py3, pycryptodomex, pyasn1, pyasn1-modules — all pure python or
# manylinux wheels). If you change pyjks version, update postinst + ucm.spec.
javaobj-py3>=0.4.3
pycryptodomex>=3.20
defusedxml>=0.7.1

# MS-XCEP / MS-WSTEP (Windows autoenrollment, GH #229): SOAP/XML envelope
# parsing and construction. Mature manylinux + Windows wheels, no native
# lib beyond libxml2 (already pulled in transitively by other deps).
lxml>=5.0
# WS-Security XML-DSig verification for WSTEP's Certificate-bound renewal
# binding (a real Windows CES client signs the RST with its current
# certificate's private key; UCM verifies that signature before treating
# it as an authenticated renewal request). Was already a transitive
# dependency via python3-saml (SAML SSO); listed directly now that WSTEP
# relies on it explicitly rather than incidentally.
xmlsec>=1.3.13

# Certificate conformance linting (RFC 5280 / CA-Browser Forum).
# Pure-python wheel; its deps (pyasn1, pyasn1-modules, cryptography) are
# already pulled in above. Optional at runtime — the linting feature
# degrades gracefully (reports "linter unavailable") if it is not installed.
pkilint>=0.12

# MS-XCEP / MS-WSTEP Kerberos binding (Phase 3, GH #229/#230):
# ADPolicyProvider_CEP_Kerberos / ADCertificateService_CES_Kerberos
# authenticate via HTTP Negotiate/SPNEGO (RFC 4559), same as IIS Windows
# Integrated Auth or Apache mod_auth_gssapi. pyspnego's Kerberos support
# always goes through the system GSSAPI implementation (there is no pure-
# Python Kerberos crypto), which needs its 'kerberos' extra — a C extension
# (gssapi) requiring libkrb5-dev at build time. Kept out of the default set
# for the same reason as requests-kerberos above. Install manually if
# needed: pip install pyspnego[kerberos]
# Both Kerberos-bound endpoints 503 cleanly if this isn't installed.
